RegistrationDto:
  title: RegistrationDto
  type: object
  properties:
    username:
      type: string
    email:
      type: string
      format: email
    password:
      type: string
      format: password
    lang:
      type: string
      default: 'en'
  required:
    - username
    - email
    - password