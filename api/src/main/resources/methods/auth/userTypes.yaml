get:
  tags:
    - User
  summary: Returns the list of user types
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../model/userTypeDto.yaml#/UserTypeDto'
  operationId: getUserTypes